# The regression contract hashes the supplier artifact byte-for-byte (#1058).
tests/fixtures/issue_1058_wheel_rh.step -text

# The independent evaluation corpus pins exact evidence bytes on every checkout (#1169).
tests/fixtures/evaluation/*.step -text
